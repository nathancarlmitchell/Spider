output to csv (escape commas in URL)
-include description: $curl.ParsedHtml.title ?
-source link: $link
-file type: $request.Headers.'Content-Type'.Split(';')[0]
-charset: $request.Headers.'Content-Type'.Split('=')[-1]
	return filetype from documentCheck()
-http response: $curl.StatusCode

request document information
-date modified

Seperate doctype from url

check if file open error

change $webcount to $linkcount

-combine formatUrl and removeHttp
-format url (replace space for Socrata)
-remove , from temp
Use socrata to convert %% back to ,

Change $path to ./- and add to report.txt
remove $path
- needed for if file exists

add link / result to error log

add docs:
dtm, ird, itl, eps, ai, 'xlsx?', agi, gci, itl, ird, glb, gdt, cel

dont export errors to main .txt

use finally to create report even if script fails

write a script description for github